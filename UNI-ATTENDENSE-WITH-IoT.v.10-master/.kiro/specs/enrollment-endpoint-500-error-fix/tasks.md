# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Bug Condition** - Second Enrollment Constraint Violation
  - **CRITICAL**: This test MUST FAIL on unfixed code - failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior - it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate the bug exists
  - **Scoped PBT Approach**: For deterministic bugs, scope the property to the concrete failing case(s) to ensure reproducibility
  - Test that enrolling a second student with a fingerprint template succeeds without constraint violation
  - Test that device_slot values are automatically assigned sequentially (0, 1, 2, ...)
  - Test that database connection is properly returned to pool after exceptions
  - Test that invalid UUID format returns 400 Bad Request (not 500)
  - Test that non-existent student_id returns 404 Not Found (not 500)
  - Test that constraint violations return 409 Conflict (not 500)
  - Run test on UNFIXED code
  - **EXPECTED OUTCOME**: Test FAILS (this is correct - it proves the bug exists)
  - Document counterexamples found to understand root cause:
    - Second enrollment fails with 500 error and database log shows "duplicate key value violates unique constraint"
    - Connection pool size decreases after exception and connection is not returned
    - Invalid UUID returns 500 instead of 400
    - Non-existent student returns 500 instead of 404
  - Mark task complete when test is written, run, and failure is documented
  - _Requirements: 1.1, 1.2, 1.3_

- [x] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - First Enrollment and Encryption Behavior
  - **IMPORTANT**: Follow observation-first methodology
  - Observe behavior on UNFIXED code for non-buggy inputs (first enrollment with valid data)
  - Write property-based tests capturing observed behavior patterns from Preservation Requirements:
    - First-time enrollment creates fingerprint template record
    - Template data is encrypted with AES-256-GCM
    - Student is marked as enrolled (fp_enrolled=TRUE)
    - Audit log entry is created
    - Response contains template_id
    - Valid base64 template data is decoded correctly
  - Property-based testing generates many test cases for stronger guarantees
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_

- [x] 3. Fix for enrollment endpoint 500 error

  - [x] 3.1 Implement the fix
    - Add UUID validation before database operations (catch ValueError → return 400 Bad Request)
    - Check student existence before enrollment (query students table, if not found → return 404 Not Found)
    - Auto-assign device_slot by querying MAX(device_slot) from fingerprint_templates and incrementing by 1
    - Replace hardcoded `device_slot = 0` with dynamic assignment: `SELECT COALESCE(MAX(device_slot), -1) + 1 FROM fingerprint_templates WHERE is_active = TRUE`
    - Wrap database operations in try-finally block to ensure connection cleanup
    - Add specific error handling:
      - Catch `psycopg2.errors.UniqueViolation` → return 409 Conflict with message "Device slot already assigned"
      - Catch `psycopg2.errors.InvalidTextRepresentation` or UUID validation errors → return 400 Bad Request with message "Invalid student_id format"
      - Catch `psycopg2.errors.ForeignKeyViolation` or check student existence first → return 404 Not Found with message "Student not found"
      - Keep generic catch-all for unexpected errors
    - _Bug_Condition: isBugCondition(input) where existingTemplateCount() >= 1 AND input.device_slot == 0 AND databaseConstraintExists("fingerprint_templates", "device_slot", "UNIQUE") AND NOT connectionCleanupInFinallyBlock() AND NOT specificErrorHandling()_
    - _Expected_Behavior: For any enrollment request, device_slot is auto-assigned, connection is cleaned up, and specific HTTP status codes are returned for different error conditions_
    - _Preservation: First-time enrollment with valid data continues to create fingerprint template records, encrypt template data with AES-256-GCM, mark student as enrolled, log audit trail, and return template_id_
    - _Requirements: 1.1, 1.2, 1.3, 2.1, 2.2, 2.3, 2.4, 2.5, 3.1, 3.2, 3.3, 3.4, 3.5_

  - [x] 3.2 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - Second Enrollment Success
    - **IMPORTANT**: Re-run the SAME test from task 1 - do NOT write a new test
    - The test from task 1 encodes the expected behavior
    - When this test passes, it confirms the expected behavior is satisfied
    - Run bug condition exploration test from step 1
    - **EXPECTED OUTCOME**: Test PASSES (confirms bug is fixed)
    - Verify second enrollment succeeds with auto-assigned device_slot
    - Verify connection pool size remains stable after exceptions
    - Verify invalid UUID returns 400 Bad Request
    - Verify non-existent student returns 404 Not Found
    - Verify constraint violations return 409 Conflict
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5_

  - [x] 3.3 Verify preservation tests still pass
    - **Property 2: Preservation** - First Enrollment and Encryption Behavior
    - **IMPORTANT**: Re-run the SAME tests from task 2 - do NOT write new tests
    - Run preservation property tests from step 2
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Confirm all tests still pass after fix (no regressions)
    - Verify first-time enrollment continues to work correctly
    - Verify template encryption remains unchanged
    - Verify student marking continues to work
    - Verify audit logging remains unchanged

- [x] 4. Checkpoint - Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise.
